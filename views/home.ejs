<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>re-ad</title>
</head>
<body>
    <div>
        <%- include("./modules/nav", {req: req })  %>
        <div>
            <h1>re-ad</h1>
            <p>the killer of reddit</p>
            <p>main communitys</p>
            <div>
                <% communitys.forEach(coms => { %>
                    <% if (coms.privacy == !0) { %>
                        <a href="community/<%= coms.id %>"><%= coms.name %></a><br>
                    <% } %>
                <% }); %>
            </div>        
        </div>
    </div>
</body>
</html>