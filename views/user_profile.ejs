<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>re-ad: </title>
</head>
<body>
    <div>
        <%- include("./modules/nav", { req: req })  %>
        <div>
            <div>
                <% user.forEach(data => { %>
                    <h1><%= data.name %></h1>
                    <p>part of:</p>
                    <% list.forEach(list => { %>
                        <% if (list.status === 2) { %>
                            <a href="/community/<%= list.com_id  %>"><%=   %></a>
                        <% } %>
                    <% }); %>
                <% }); %>
            </div>        
        </div>
    </div>
</body>
</html>